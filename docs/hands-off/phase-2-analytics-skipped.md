# Carte Hands-off: Phase 2 访问统计（跳过）

**状态**: 已根据产品 Owner 决定跳过，本阶段无代码变更
**日期**: 2026-09-04
**依据**: `docs/tech-spec-detailed.md` Phase 2 功能路线及产品 Owner 最新决策

## 决策

访问统计当前不是必要能力，暂不开发独立的统计模块、趋势图、访客去重、点击追踪或转化率计算。下一阶段直接进入 RSVP 可靠性增强与通知；更多模板继续放在本轮计划最后。

## 保留的既有能力

- `invitations.viewCount` 和公开页访问时的基础累加逻辑保持不变。
- Dashboard 已有的累计浏览量展示保持不变，作为基础运营信息，不扩展为独立分析产品。
- 本次不新增 Prisma 模型、迁移、API、前端页面或统计任务。

## 范围影响

- 不定义访客去重规则、设备/IP 识别、时间序列维度或转化率分母。
- 不新增访问统计专用 E2E 或服务器部署验收，因为没有实现变更。
- 后续如重新启用该需求，需要重新确认指标定义、隐私边界、数据保留周期和存储方案，并单独建立开发阶段。

## 验证与提交

本阶段仅更新路线图和交接文档，未启动本地应用、Docker 或测试服务器。文档变更通过 `git diff --check` 校验。

提交：`docs: skip phase 2 analytics`

## 下一阶段

进入 RSVP 可靠性增强与通知，重点确认重复提交、限流、通知触发条件和失败重试规则；更多模板仍为本轮最后阶段。

